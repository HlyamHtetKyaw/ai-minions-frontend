/**
 * Class string for a dashed “add / create” tile. Use on `Link` or `button`/`a` as `className`.
 * Compose with `min-h-*` or padding overrides if needed.
 */
export const dashboardActionTileClassName =
  'flex min-h-[280px] flex-col items-center justify-center gap-3 rounded-2xl border border-dashed border-white/20 bg-black/10 px-6 py-10 text-center transition-colors hover:border-emerald-500/40 hover:bg-emerald-500/6 dark:bg-white/2';
