/**
 * Max height (px) for the main preview video in CreationStudio (viral shorts).
 * Workspace video editor uses the same cap so preview scale matches.
 */
export const STUDIO_PREVIEW_MAX_VIDEO_HEIGHT_PX = 420;
