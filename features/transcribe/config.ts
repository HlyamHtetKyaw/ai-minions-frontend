import { Mic } from 'lucide-react';

export const config = {
  key: 'transcribe',
  name: 'Transcribe',
  description: 'Convert audio and video speech to accurate text.',
  icon: Mic,
  href: '/transcribe',
} as const;
