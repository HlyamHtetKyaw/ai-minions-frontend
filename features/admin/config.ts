import { Settings } from 'lucide-react';

export const config = {
  key: 'admin',
  name: 'Admin',
  description: 'Manage users, pricing, and platform settings.',
  icon: Settings,
  href: '/admin',
} as const;
