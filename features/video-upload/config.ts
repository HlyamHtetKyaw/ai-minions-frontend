import { Upload } from 'lucide-react';

export const config = {
  key: 'video-upload',
  name: 'Video Upload',
  description: 'Upload and manage video files for processing.',
  icon: Upload,
  href: '/video-upload',
} as const;
